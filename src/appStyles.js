import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  underline: {
    width: "5rem",
    height: "0.25rem",
    background: "#5f6cc5",
    marginLeft: "auto",
    marginRight: "auto",
  },
  title: {
    textAlign: "center",
    marginBottom: "2rem",
  },
}));
